{
  "commands": [
    "git status",
    "git log --oneline -20",
    "git diff",
    "git diff --staged",
    "git branch",
    "git branch -a",
    "git stash list",
    "git show --stat HEAD",
    { "command": "cd frontend; npm run build", "description": "切換到 frontend 目錄並執行 build" }
  ]
}
